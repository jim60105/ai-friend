services:
  ai-friend:
    container_name: ai-friend
    image: ghcr.io/jim60105/ai-friend:latest
    build:
      context: .
      dockerfile: Containerfile
      target: final
    env_file:
      - .env
    volumes:
      - ./data:/app/data:Z
      - ./config.yaml:/app/config.yaml:ro,Z
