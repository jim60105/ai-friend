name: Docker Publish Latest

on:
  push:
    branches:
      - master
  workflow_dispatch:

# Sets the permissions granted to the GITHUB_TOKEN for the actions in this job.
permissions:
  contents: read
  packages: write
  id-token: write
  attestations: write

jobs:
  docker-latest:
    name: Build and Push Docker Image (latest)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Docker
        id: setup
        uses: ./.github/workflows/docker-reused-setup-steps
        with:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}
          QUAY_USERNAME: ${{ secrets.QUAY_USERNAME }}
          QUAY_TOKEN: ${{ secrets.QUAY_TOKEN }}
          tag: latest

      - name: Build and push
        id: build_push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Containerfile
          push: true
          target: final
          tags: ${{ steps.setup.outputs.tags }}
          labels: ${{ steps.setup.outputs.labels }}
          build-args: |
            VERSION=latest
            RELEASE=${{ github.run_number }}
          platforms: linux/amd64,linux/arm64
          # Cache to registry instead of gha to avoid the capacity limit.
          cache-from: type=registry,ref=ghcr.io/${{ github.repository_owner }}/agent-chatbot:cache
          cache-to: type=registry,ref=ghcr.io/${{ github.repository_owner }}/agent-chatbot:cache,mode=max
          sbom: true
          provenance: true

      - name: Add attestations for GitHub Container Registry
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: ghcr.io/${{ github.repository_owner }}/agent-chatbot
          subject-digest: ${{ steps.build_push.outputs.digest }}

      - name: Add attestations for DockerHub
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: ${{ secrets.DOCKERHUB_USERNAME }}/agent-chatbot
          subject-digest: ${{ steps.build_push.outputs.digest }}

      - name: Add attestations for Quay
        uses: actions/attest-build-provenance@v2
        with:
          subject-name: quay.io/${{ github.repository_owner }}/agent-chatbot
          subject-digest: ${{ steps.build_push.outputs.digest }}
