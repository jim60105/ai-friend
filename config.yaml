# config.yaml - Default configuration for Agent Chatbot
# Environment variables can override these values

platforms:
  discord:
    token: "${DISCORD_TOKEN}"
    enabled: true
  misskey:
    host: "${MISSKEY_HOST}"
    token: "${MISSKEY_TOKEN}"
    enabled: false

agent:
  model: "gpt-4"
  systemPromptPath: "./prompts/system.md"
  tokenLimit: 4096

memory:
  searchLimit: 10
  maxChars: 2000
  recentMessageLimit: 20

workspace:
  repoPath: "./data"
  workspacesDir: "workspaces"

logging:
  level: "INFO"
